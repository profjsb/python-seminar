import numpy as np
import pylab
'''This is a fairly useless program which uses pylab.rand() to generate some 
data.  

Example Usage:
num_to_generate = 500
data = pylab.rand(num_to_generate)

"""
'''
def randData(num_to_generate):
    mydata = pylab.rand(num_to_generate)
    return mydata

